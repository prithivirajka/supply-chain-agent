packages:
  # dbt utility macros (date_spine, surrogate_key, safe_divide, etc.)
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]

  # Data quality testing (expect_* tests)
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<1.0.0"]

  # Audit helper — compare row counts & results across runs
  - package: dbt-labs/audit_helper
    version: [">=0.9.0", "<1.0.0"]
