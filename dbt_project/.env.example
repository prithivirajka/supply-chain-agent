# Copy this file to .env and fill in your values.
# Never commit the actual .env file.

# Path to the DuckDB file (relative to the project root)
DBT_DUCKDB_PATH=../data/supply_chain.duckdb

# Source schema populated by load_raw_data.py
DBT_SOURCE_DB=supply_chain
DBT_SOURCE_SCHEMA=raw
